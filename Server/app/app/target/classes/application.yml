spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/medreminder_new
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update   # создаёт таблицы автоматически
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080